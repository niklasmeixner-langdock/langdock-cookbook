{
  "name": "agent-embed-iframe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Iframe chat widget in front of the Langdock Agent API. The browser never sees the API key.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
